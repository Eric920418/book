// Prisma schema for MAIA-2 內感受量表系統
// 《你不是破碎，而是入口》書籍網站

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 管理員資料表
model Admin {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String   // 使用 bcrypt 雜湊
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("admins")
}

// MAIA-2 測驗結果資料表
// 儲存用戶主動分享的測驗結果（填寫 email 者）
model MAIA2Result {
  id              String   @id @default(cuid())
  email           String   // 用戶填寫的 email

  // 8 個覺察面向分數（0-5分）
  dimensionScores Json     // { noticing: 3.25, notDistracting: 2.83, ... }

  // 測驗完成時間
  completedAt     DateTime

  // 其他元數據
  metadata        Json?    // { userAgent, submittedAt, ... }

  // 創建時間（資料庫記錄時間）
  createdAt       DateTime @default(now())

  @@index([email])
  @@index([createdAt])
  @@map("maia2_results")
}

// 未來擴展：如果需要記錄詳細的 37 題答案，可以新增以下 model
// model MAIA2Answer {
//   id          String       @id @default(cuid())
//   result      MAIA2Result  @relation(fields: [resultId], references: [id], onDelete: Cascade)
//   resultId    String
//   questionId  Int          // 1-37
//   score       Int          // 0-5（原始分數，不含反向計分）
//   createdAt   DateTime     @default(now())
//   @@map("maia2_answers")
// }
